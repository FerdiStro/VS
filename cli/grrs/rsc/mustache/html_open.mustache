<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            margin: 0;
            transform: scale(2.6);
            transform-origin: top left;
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
        }

        #footer{
            position: fixed;
            height: 60px;
            top: 840px;
            display: flex;
        }

        #header {
            display: flex;
            align-items: center;
            margin: 0;

        }

        .contact-data {
            width: 150px;
            position: relative;
            font-size: 10px;
            top: 25px;
            margin-left: 12px;

        }

        #name {
            width: 100%;
            text-align: center;
            margin-top: 20px;
        }

        .up {
            position: relative;
            width: 0;
            height: 0;
            border-style: inset;
            border-width: 0 200px 100px 200px;
            border-color: transparent transparent {{color}} transparent;
            transform: rotate(180deg);
            margin-left: -62px;
        }

        .up img {
            transform: rotate(180deg);
            width: 70px;
            height: 70px;
            border-radius: 130px;
            position: relative;
            top: 18px;
            left: -35px;
        }

        .down {
            width: 0;
            height: 0;
            border-style: inset;
            border-width: 0 100px 60px 100px;
            border-color: transparent transparent {{color}} transparent;
            float: left;
            position: relative;
            margin-left: 38px;
        }

        .down p {
            position: relative;
            top: 20px;
            left: -49px;
            font-size: 9px;
        }
        .box-big{
            margin-left: 20px;
            width: 380px;
        }
        .box-small{
            margin-left: 20px;
            width: 170px;
        }
        .box-header{

            font-size: 15px;
            border-bottom: 1px solid grey;
        }
        .box-content{
            position: relative;
            top: -10px;
            font-size: 11px;
        }

        .rounded-circle-fill {
            display: inline-block;
            width: 5px;
            height: 5px;
            background-color: {{color}};
            border-radius: 50%;
            margin-left: 1px;
        }

        .rounded-circle-non-fill {
            display: inline-block;
            width: 5px;
            height: 5px;
            background-color: rgba(160, 198, 241, 0.55);;
            border-radius: 50%;
            margin-left: 1px;
        }
        .point{
            width: 105px;
            display: inline-block;
        }
        .value{
            display: inline;
        }


    </style>
</head>
<body>
    <div style="width: 600px; height: 850px;  ">


