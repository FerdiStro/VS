openapi: 3.0.0
info:
  version: "0.0.1"
  title: ""
paths:
  /application:
    get:
      tags:
        - application
      description: "GET existing application with Id"
      parameters:
        - in: query
          name: userId
          schema:
            type: string
      responses:
        "200":
          description: "Success"